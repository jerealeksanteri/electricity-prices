# ENTSO-E API security token (required). Request one at https://transparency.entsoe.eu/
ENTSO_E_TOKEN=
# Optional bind address. Leave UNSET for `dx serve` (the Dioxus CLI assigns the
# dev port). Set it for production / Docker (the image sets 0.0.0.0:8080).
#BIND_ADDR=0.0.0.0:8080